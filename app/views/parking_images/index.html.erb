<% @parking_image.each do |parking_image| %>
  <%= attachment_image_tag parking_image,:image, :fill, 100, 100%>
  <%= link_to "削除", parking_path(parking_image), method: :delete, data: { confirm: '本当に削除しますか?' } %>
<% end %>
