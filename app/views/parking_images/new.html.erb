<% @parking_images.each do |parking_image| %>
  <%= attachment_image_tag parking_image,:image, :fill, 100, 100%>
  <%= link_to "削除", parking_parking_image_path(@parking, parking_image), method: :delete, data: { confirm: '本当に削除しますか?' } %>
<% end %>



<%=form_for [@parking,@parking_image] do |image|%>

 <%= image.attachment_field :image %>

<%= image.submit '写真の追加'%>

<% end %>
<%= link_to "戻る", park_list_path %>