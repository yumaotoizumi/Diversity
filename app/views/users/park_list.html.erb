<table>
	<thead>
		<tr>
			<th>ユーザーid</th>
			<th>パーキングid</th>
			<th>カータイプ</th>
			<th>値段（１日の料金）</th>
			<th>消去</th>
		</tr>
	</thead>
	<tbody>
		<% @user.parkings.each do |parking| %>
		<tr>
			<td><%= parking.user.id %></td>
			<td><%= parking.id %></td>
			<td><%= parking.car_model %></td>
			<td><%= parking.price %></td>
			<td><%= link_to "削除", parking_path(@user.parkings), method: :delete %></td>
		</tr>
		<%end%>
	</tbody>
</table>